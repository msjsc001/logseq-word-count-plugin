{
  "name": "logseq-word-count-plugin",
  "version": "1.0.12",
  "logseq": {
    "id": "logseq-word-count-plugin",
    "icon": "icon.svg",
    "main": "dist/index.html"
  },
  "scripts": {
    "build": "parcel build index.html --dist-dir dist || true && node copy-files.js"
  },
  "devDependencies": {
    "@logseq/libs": "^0.0.15",
    "@parcel/transformer-typescript-types": "latest",
    "@types/node": "latest",
    "eslint": "^9.25.1",
    "parcel": "^2.9.3",
    "typescript": "^5.8.3"
  }
}
